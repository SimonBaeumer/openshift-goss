# OpenShift cli container

Includes openshift-cli, gitcrypt and docker.

## Build image

`docker build -t simonbaeumer/docker-openshift .`

## Run image

`docker run simonbaeumer/docker-openshift` 

## Available tags

 - `3.10.0-gitcrypt`, `3.11.0-gitcrypt`